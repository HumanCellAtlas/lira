apiVersion: v1
kind: Service
metadata:
    name: {{ env "SERVICE_NAME" }}
spec:
    type: NodePort
    selector:
        app: {{ env "APPLICATION_NAME" }}
    ports:
        - port: 8080
          targetPort: 8080
          protocol: TCP
